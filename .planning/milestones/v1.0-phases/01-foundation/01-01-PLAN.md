---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/index.tsx
  - src/app/styles/index.css
  - src/pages/home/ui/PlottedApp.tsx
  - src/pages/home/index.ts
  - package.json
  - vite.config.ts
  - playwright.config.ts
autonomous: true
requirements: [SETUP-01, SETUP-04]
must_haves:
  truths:
    - "アプリが bun run build でエラーなくビルドできる"
    - "bun run typecheck が成功する"
    - "Convex, Clerk, wouter, next-themes のインポートがソースコードに一切存在しない"
    - "ページ背景が羊皮紙ベージュ (#F5F0E8)、テキストがダークブラウン (#2C2416) で表示される"
    - "shadcn/ui コンポーネントがノワールテーマのカラーで表示される"
    - "左上に「Plotted.」タイトルが小さく表示される"
  artifacts:
    - path: "src/app/index.tsx"
      provides: "Minimal entry point with ErrorBoundary + PlottedApp"
      contains: "PlottedApp"
    - path: "src/app/styles/index.css"
      provides: "Noir theme CSS variables mapped to shadcn contract"
      contains: "--background"
    - path: "src/pages/home/ui/PlottedApp.tsx"
      provides: "Minimal page with Plotted. title"
      contains: "Plotted."
  key_links:
    - from: "src/app/index.tsx"
      to: "src/pages/home"
      via: "import PlottedApp"
      pattern: "import.*PlottedApp.*from"
    - from: "src/app/styles/index.css"
      to: "shadcn components"
      via: "CSS variable contract (--background, --foreground, --primary, etc.)"
      pattern: "--background.*oklch"
---

<objective>
Clean the codebase by removing all Convex/Clerk/Todo/wouter/next-themes code, then apply the classic noir design theme to the entire app via Tailwind CSS v4 tokens and shadcn CSS variable mapping.

Purpose: Establish a clean, buildable foundation with the correct visual identity before adding any Plotted-specific features.
Output: A minimal running app with noir theme, no dead code, and a single PlottedApp page.
</objective>

<execution_context>
@/Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/plotted/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/plotted/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/1-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Todoアプリ・Convex・Clerk・wouter・next-themes関連コードの全削除とエントリーポイント再構築 (SETUP-01, D-01, D-02, D-03, D-11)</name>
  <files>
    src/app/index.tsx,
    src/pages/home/ui/PlottedApp.tsx,
    src/pages/home/index.ts,
    package.json,
    vite.config.ts,
    playwright.config.ts
  </files>
  <read_first>
    src/app/index.tsx,
    src/app/routes/index.ts,
    src/app/routes/routes.tsx,
    src/pages/home/ui/HomePage.tsx,
    src/pages/home/index.ts,
    src/widgets/header/index.ts,
    src/widgets/header/ui/Header.tsx,
    src/features/todo/index.ts,
    src/shared/api/index.ts,
    package.json,
    vite.config.ts,
    playwright.config.ts,
    e2e/global.setup.ts,
    e2e/todo.spec.ts,
    .env.dev,
    .env.prod
  </read_first>
  <action>
    **1. ディレクトリ・ファイル削除:**
    - `rm -rf src/features/todo/` (Todo feature slice全体)
    - `rm -rf src/shared/api/` (Convex schema, mutations, generated types, barrel)
    - `rm -rf src/widgets/header/` (ヘッダーウィジェット — D-01でヘッダーなし)
    - `rm -rf src/app/routes/` (ルーティング層 — D-03でwouter削除)
    - `rm e2e/todo.spec.ts` (Todo用E2Eテスト)
    - `rm e2e/global.setup.ts` (Clerk auth setup)

    **2. package.json 編集:**
    dependencies から削除:
    - `"convex"` (バックエンド削除)
    - `"@clerk/react"` (認証削除)
    - `"wouter"` (ルーティング削除 — D-03)
    - `"next-themes"` (ダークモード削除 — D-11)
    - `"@fontsource-variable/noto-sans"` (システムフォント使用 — D-02)

    devDependencies から削除:
    - `"@clerk/testing"` (Clerkテスト)
    - `"@cloudflare/vite-plugin"` (Workers不使用)
    - `"wrangler"` (Workers CLI不使用)
    - `"@dotenvx/dotenvx"` (env vars不要)

    scripts 編集:
    - `"dev"` を `"vite"` に変更 (dotenvx削除)
    - `"preview"` を `"vite build && vite preview"` に変更
    - `"test"` を `"vitest --project unit & vitest --project browser"` に変更 (playwright E2E除去)
    - `"check"` を `"bun typecheck && bun run build"` に変更 (wrangler deploy除去)
    - `"cf-typegen"` を削除
    - `"deploy"` を削除

    `"cloudflare"` セクション全体を削除。
    `"name"` を `"plotted"` に変更。
    `"description"` を `"Murder mystery alibi tracker"` に変更。

    **3. vite.config.ts 編集:**
    - `import { cloudflare } from "@cloudflare/vite-plugin";` を削除
    - `plugins` 配列から `cloudflare()` を削除 → `plugins: [react(), tailwindcss()]`

    **4. src/app/index.tsx 書き換え:**
    ```tsx
    import "./styles/index.css";
    import { StrictMode } from "react";
    import { createRoot } from "react-dom/client";

    import { ErrorBoundary } from "@/app/providers/ErrorBoundary";
    import { PlottedApp } from "@/pages/home";

    createRoot(document.getElementById("root")!).render(
      <StrictMode>
        <ErrorBoundary>
          <PlottedApp />
        </ErrorBoundary>
      </StrictMode>,
    );
    ```

    **5. src/pages/home/ui/PlottedApp.tsx 新規作成 (D-01, D-02):**
    ```tsx
    export const PlottedApp = () => {
      return (
        <div className="min-h-screen bg-background p-4">
          <span className="text-sm font-medium text-foreground">Plotted.</span>
        </div>
      );
    };
    ```

    **6. src/pages/home/index.ts バレルファイル更新:**
    ```ts
    export { PlottedApp } from "./ui/PlottedApp";
    ```

    **7. 既存の src/pages/home/ui/HomePage.tsx を削除。**

    **8. playwright.config.ts 編集:**
    - Clerk auth setup (`globalSetup`) への参照を削除
    - テスト対象を `e2e/**/*.spec.ts` のまま維持 (e2eディレクトリは残す)

    **9. .env.dev, .env.prod 編集:**
    - `VITE_CONVEX_URL` を削除
    - `VITE_CLERK_PUBLISHABLE_KEY` を削除
    - Clerk/Convex関連のすべての環境変数を削除

    **10. wrangler.jsonc は残す (削除は低優先度、ビルドに影響なし)。**

    **11. `bun install` を実行して lockfile を更新。**

  </action>
  <verify>
    <automated>cd /Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/plotted && bun run typecheck && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "convex" src/` が0件 (convex参照なし)
    - `grep -r "@clerk" src/` が0件 (clerk参照なし)
    - `grep -r "wouter" src/` が0件 (wouter参照なし)
    - `grep -r "next-themes" src/` が0件 (next-themes参照なし)
    - `grep -r "fontsource" src/` が0件 (Noto Sans参照なし)
    - `src/pages/home/ui/PlottedApp.tsx` が存在し `Plotted.` を含む
    - `src/app/index.tsx` が `PlottedApp` をインポートし、Clerk/Convexプロバイダーを含まない
    - `package.json` に `convex`, `@clerk/react`, `wouter`, `next-themes` が含まれない
    - `vite.config.ts` に `cloudflare` が含まれない
    - `bun run typecheck` が exit 0
    - `bun run build` が exit 0
  </acceptance_criteria>
  <done>Convex/Clerk/Todo/wouter/next-themes関連コードが全て削除され、PlottedAppを直接レンダリングするミニマルなエントリーポイントでビルドが通る</done>
</task>

<task type="auto">
  <name>Task 2: ノワールテーマのCSS変数定義とshadcn変数マッピング (SETUP-04, D-08, D-09, D-10, D-11)</name>
  <files>
    src/app/styles/index.css
  </files>
  <read_first>
    src/app/styles/index.css,
    .planning/PROJECT.md
  </read_first>
  <action>
    `src/app/styles/index.css` を以下の内容で完全に書き換える。

    **CSSファイル全体の構造:**

    ```css
    @import "tailwindcss";
    @import "tw-animate-css";
    @import "shadcn/tailwind.css";

    @theme inline {
      --radius-sm: calc(var(--radius) - 4px);
      --radius-md: calc(var(--radius) - 2px);
      --radius-lg: var(--radius);
      --radius-xl: calc(var(--radius) + 4px);
      --radius-2xl: calc(var(--radius) + 8px);
      --radius-3xl: calc(var(--radius) + 12px);
      --radius-4xl: calc(var(--radius) + 16px);

      /* shadcn bridge — maps CSS vars to Tailwind utility classes */
      --color-background: var(--background);
      --color-foreground: var(--foreground);
      --color-card: var(--card);
      --color-card-foreground: var(--card-foreground);
      --color-popover: var(--popover);
      --color-popover-foreground: var(--popover-foreground);
      --color-primary: var(--primary);
      --color-primary-foreground: var(--primary-foreground);
      --color-secondary: var(--secondary);
      --color-secondary-foreground: var(--secondary-foreground);
      --color-muted: var(--muted);
      --color-muted-foreground: var(--muted-foreground);
      --color-accent: var(--accent);
      --color-accent-foreground: var(--accent-foreground);
      --color-destructive: var(--destructive);
      --color-border: var(--border);
      --color-input: var(--input);
      --color-ring: var(--ring);

      /* Semantic noir tokens (D-10) — usable as bg-bg, text-ink-dark, etc. */
      --color-bg: var(--background);
      --color-surface: var(--secondary);
      --color-card-noir: var(--card);
      --color-border-gold: var(--border);
      --color-border-light: var(--input);
      --color-ink-dark: var(--foreground);
      --color-ink-mid: oklch(0.42 0.045 68);
      --color-ink-muted: var(--muted-foreground);
      --color-accent-dark: var(--primary);
      --color-accent-pale: var(--accent);

      /* Border radius tokens per UIルール */
      --radius-card: 10px;
      --radius-button: 6px;
      --radius-chip: 999px;

      /* Font — system sans-serif (D-02) */
      --font-sans: system-ui, -apple-system, sans-serif;
    }

    :root {
      --radius: 0.625rem;

      /* Noir palette mapped to shadcn CSS variable contract */
      --background: oklch(0.96 0.018 78);       /* #F5F0E8 羊皮紙ベージュ */
      --foreground: oklch(0.18 0.030 68);        /* #2C2416 ダークブラウン */
      --card: oklch(0.98 0.012 78);              /* #FAF6EE オフホワイト */
      --card-foreground: oklch(0.18 0.030 68);   /* #2C2416 */
      --popover: oklch(0.98 0.012 78);           /* #FAF6EE */
      --popover-foreground: oklch(0.18 0.030 68); /* #2C2416 */
      --primary: oklch(0.38 0.075 56);           /* #7A4F2A ダークアンバー */
      --primary-foreground: oklch(0.92 0.035 82); /* #F0E4CC ペールアンバー */
      --secondary: oklch(0.93 0.020 78);         /* #EDE7D9 くすんだクリーム */
      --secondary-foreground: oklch(0.18 0.030 68); /* #2C2416 */
      --muted: oklch(0.93 0.020 78);             /* #EDE7D9 */
      --muted-foreground: oklch(0.60 0.040 70);  /* #9C8B6E ミュートブラウン */
      --accent: oklch(0.92 0.035 82);            /* #F0E4CC ペールアンバー */
      --accent-foreground: oklch(0.38 0.075 56); /* #7A4F2A ダークアンバー */
      --destructive: oklch(0.577 0.245 27.325);  /* existing red */
      --border: oklch(0.77 0.040 78);            /* #C8B99A アンティークゴールド */
      --input: oklch(0.85 0.025 78);             /* #DDD3BE ライトベージュ */
      --ring: oklch(0.38 0.075 56);              /* #7A4F2A */
    }

    @layer base {
      * {
        @apply border-border outline-ring/50;
      }
      body {
        @apply h-full bg-background font-sans text-foreground;
      }
      html {
        @apply h-full font-sans;
      }
    }
    ```

    **重要な変更点:**
    - `@import "@fontsource-variable/noto-sans";` を削除 (D-02: システムフォント使用)
    - `@custom-variant dark (&:is(.dark *));` を削除 (D-11: ダークモードなし)
    - `.dark { ... }` ブロック全体を削除 (D-11)
    - `:root` の全色値をノワールパレットのOKLCH値に置き換え (D-08, D-09)
    - sidebar系の変数は不要なので削除 (サイドバーなし)
    - chart系の変数は不要なので削除 (チャートなし)
    - `--font-sans` をシステムフォントに変更 (D-02)
    - `@theme inline` にセマンティックトークン (`--color-bg`, `--color-ink-dark` 等) を追加 (D-10)
    - `@theme inline` にボーダー半径トークン (`--radius-card`, `--radius-button`, `--radius-chip`) を追加
    - `@layer base` の重複行を除去

    **OKLCH値は01-RESEARCHの近似値を使用。** ブラウザで目視確認し、色が明らかに異なる場合は oklch.com で正確な値を取得して修正する。

  </action>
  <verify>
    <automated>cd /Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/plotted && bun run typecheck && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/styles/index.css` に `--background: oklch(0.96 0.018 78)` が含まれる (羊皮紙ベージュ)
    - `src/app/styles/index.css` に `--foreground: oklch(0.18 0.030 68)` が含まれる (ダークブラウン)
    - `src/app/styles/index.css` に `--primary: oklch(0.38 0.075 56)` が含まれる (ダークアンバー)
    - `src/app/styles/index.css` に `--color-bg: var(--background)` が含まれる (セマンティックトークン)
    - `src/app/styles/index.css` に `--color-ink-dark: var(--foreground)` が含まれる
    - `src/app/styles/index.css` に `--color-surface: var(--secondary)` が含まれる
    - `src/app/styles/index.css` に `--radius-card: 10px` が含まれる
    - `src/app/styles/index.css` に `--radius-button: 6px` が含まれる
    - `src/app/styles/index.css` に `--font-sans: system-ui` が含まれる
    - `grep "fontsource" src/app/styles/index.css` が0件 (Noto Sans削除済み)
    - `grep "@custom-variant dark" src/app/styles/index.css` が0件 (ダークモード削除済み)
    - `grep "\.dark" src/app/styles/index.css` が0件 (.darkブロック削除済み)
    - `bun run build` が exit 0
  </acceptance_criteria>
  <done>ノワールテーマがCSS変数として定義され、shadcnの変数体系にマッピングされ、セマンティックトークンがTailwindユーティリティクラスとして使用可能な状態。ダークモード・Noto Sansフォント・不要なsidebar/chart変数が除去されている</done>
</task>

</tasks>

<verification>
1. `bun run typecheck` が成功する
2. `bun run build` が成功する
3. `grep -r "convex\|@clerk\|wouter\|next-themes\|fontsource" src/` が0件
4. `bun run dev` で起動し、ブラウザで羊皮紙ベージュ背景 + ダークブラウンテキストの「Plotted.」が表示される
</verification>

<success_criteria>

- Convex/Clerk/Todo/wouter/next-themes のコードが完全に除去され、ビルドが通る
- ノワールテーマがCSS変数として定義され、shadcnコンポーネントに適用される
- セマンティックトークン (bg-bg, text-ink-dark 等) がTailwindで使用可能
- 左上に「Plotted.」タイトルが表示されるミニマルなページが動作する
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
