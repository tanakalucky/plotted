---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/shared/model/state.ts
  - src/shared/model/reducer.ts
  - src/shared/model/use-app-state.ts
  - src/shared/model/use-map-images.ts
  - src/shared/model/index.ts
  - src/shared/model/state.unit.test.ts
autonomous: true
requirements: [SETUP-02, SETUP-03]
must_haves:
  truths:
    - "useAppState フックが useReducer + localStorage 自動保存・自動復元を提供する"
    - "リロード後に保存した状態が復元される"
    - "localStorage の JSON が壊れている場合、サイレントに初期状態で起動する"
    - "トップレベルキーが欠けている場合、サイレントに初期状態で起動する"
    - "IndexedDB にマップ画像 Blob を保存・取得・削除できる"
    - "IndexedDB アクセス不可時でもアプリがクラッシュしない"
  artifacts:
    - path: "src/shared/model/state.ts"
      provides: "State, PlotLog, MapDef, CharDef types and initialState"
      exports: ["State", "PlotLog", "MapDef", "CharDef", "initialState"]
    - path: "src/shared/model/reducer.ts"
      provides: "Action union type and reducer function"
      exports: ["Action", "reducer"]
    - path: "src/shared/model/use-app-state.ts"
      provides: "useAppState hook with localStorage persistence"
      exports: ["useAppState"]
    - path: "src/shared/model/use-map-images.ts"
      provides: "IndexedDB image CRUD via idb-keyval"
      exports: ["saveMapImage", "loadMapImage", "deleteMapImage"]
    - path: "src/shared/model/index.ts"
      provides: "Barrel file for shared/model"
  key_links:
    - from: "src/shared/model/use-app-state.ts"
      to: "src/shared/model/reducer.ts"
      via: "import reducer and Action"
      pattern: "import.*reducer.*from"
    - from: "src/shared/model/use-app-state.ts"
      to: "localStorage"
      via: "useEffect auto-save on state change"
      pattern: "localStorage\\.setItem"
    - from: "src/shared/model/use-map-images.ts"
      to: "idb-keyval"
      via: "get/set/del imports"
      pattern: "import.*from.*idb-keyval"
---

<objective>
Implement the core state model (useReducer), localStorage auto-persistence, and IndexedDB image storage layer that all future features will build upon.

Purpose: Every Phase 2/3 feature dispatches actions and reads state through this foundation. The persistence layer ensures data survives page reloads.
Output: State type definitions, reducer, useAppState hook, and idb-keyval image utilities in `src/shared/model/`.
</objective>

<execution_context>
@/Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/plotted/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/plotted/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/1-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- State type from PROJECT.md — the canonical data model -->

```typescript
// src/shared/model/state.ts (to be created)
export interface PlotLog {
  id: number;
  char: string;
  map: string;
  day: number;
  time: number; // 0-143 (10-minute index)
  x: number; // 0.0-1.0
  y: number; // 0.0-1.0
}

export interface MapDef {
  id: string;
  name: string;
  img: string | null; // map ID key into IndexedDB
}

export interface CharDef {
  name: string;
  color: string;
}

export interface State {
  chars: CharDef[];
  maps: MapDef[];
  logs: PlotLog[];
  days: number; // 1-7
  activeChar: string | null;
  activeDay: number;
  currentTime: number; // 0-143
}
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: State型定義・reducer・useAppState hookの実装 (SETUP-02, D-04, D-05, D-07)</name>
  <files>
    src/shared/model/state.ts,
    src/shared/model/reducer.ts,
    src/shared/model/use-app-state.ts,
    src/shared/model/index.ts,
    src/shared/model/state.unit.test.ts
  </files>
  <read_first>
    .planning/PROJECT.md,
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - isValidState: トップレベルキー7つ (chars, maps, logs, days, activeChar, activeDay, currentTime) が全て存在する場合 true を返す
    - isValidState: キーが1つでも欠けている場合 false を返す
    - isValidState: null や非オブジェクトで false を返す
    - reducer RESET: initialState を返す
    - reducer unknown action: 現在の state をそのまま返す
    - loadState: localStorage が空の場合 initialState を返す
    - loadState: 正常な JSON が保存されている場合 パースした State を返す
    - loadState: 壊れた JSON (パースエラー) の場合 initialState を返す (D-04)
    - loadState: キーが欠けた JSON の場合 initialState を返す (D-05)
  </behavior>
  <action>
    **1. `src/shared/model/state.ts` を作成:**
    - `PlotLog`, `MapDef`, `CharDef`, `State` の interface 定義 (上記 `<interfaces>` ブロックの通り)
    - `initialState` 定数: `{ chars: [], maps: [], logs: [], days: 1, activeChar: null, activeDay: 1, currentTime: 0 }`
    - `isValidState(v: unknown): v is State` 関数: `typeof v === "object" && v !== null` かつ `["chars", "maps", "logs", "days", "activeChar", "activeDay", "currentTime"]` の7キー全てが `in` で存在チェック (D-05: Valibotは使わない)

    **2. `src/shared/model/reducer.ts` を作成:**
    - `Action` 型: `{ type: "RESET" }` (Phase 1では RESET のみ。Phase 2以降で CHAR_ADD 等を union に追加)
    - `reducer(state: State, action: Action): State` 関数: `switch (action.type)` で `"RESET"` は `initialState` を返す、`default` は `state` を返す

    **3. `src/shared/model/use-app-state.ts` を作成:**
    - `STORAGE_KEY = "plotted-v1"` 定数
    - `loadState(): State` 関数:
      - `localStorage.getItem(STORAGE_KEY)` で取得
      - null なら `initialState` を返す
      - `JSON.parse` を try-catch で実行、失敗なら `initialState` (D-04: サイレントリセット)
      - `isValidState` でチェック、失敗なら `initialState` (D-05)
      - 成功なら `parsed as State` を返す
    - `useAppState()` hook:
      - `const [state, dispatch] = useReducer(reducer, undefined, loadState)`
      - `useEffect(() => { localStorage.setItem(STORAGE_KEY, JSON.stringify(state)); }, [state])` で自動保存
      - `return { state, dispatch } as const` を返す

    **4. `src/shared/model/index.ts` バレルファイル:**
    ```ts
    export { type State, type PlotLog, type MapDef, type CharDef, initialState, isValidState } from "./state";
    export { type Action, reducer } from "./reducer";
    export { useAppState } from "./use-app-state";
    export { saveMapImage, loadMapImage, deleteMapImage } from "./use-map-images";
    ```

    **5. `src/shared/model/state.unit.test.ts` テスト:**
    - `isValidState` の正常系・異常系テスト
    - `reducer` RESET テスト
    - `loadState` の localStorage 空・正常・破損・キー欠損テスト (localStorage をモックして)

  </action>
  <verify>
    <automated>cd /Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/plotted && bun run typecheck && bunx vitest run --project unit src/shared/model/state.unit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/shared/model/state.ts` に `export interface State` が含まれる
    - `src/shared/model/state.ts` に `export const initialState` が含まれる
    - `src/shared/model/state.ts` に `export const isValidState` が含まれる
    - `src/shared/model/reducer.ts` に `export type Action` が含まれる
    - `src/shared/model/reducer.ts` に `export const reducer` が含まれる
    - `src/shared/model/use-app-state.ts` に `useReducer` が含まれる
    - `src/shared/model/use-app-state.ts` に `localStorage.setItem` が含まれる
    - `src/shared/model/use-app-state.ts` に `localStorage.getItem` が含まれる
    - `src/shared/model/use-app-state.ts` に `isValidState` が含まれる
    - `src/shared/model/state.unit.test.ts` のテストが全て pass する
    - `bun run typecheck` が exit 0
  </acceptance_criteria>
  <done>State型・reducer・useAppState hookが定義され、localStorage自動保存/復元が動作する。破損データはサイレントリセットされる。テストで検証済み</done>
</task>

<task type="auto">
  <name>Task 2: idb-keyvalインストールとIndexedDBマップ画像ストレージ実装 (SETUP-03, D-06)</name>
  <files>
    src/shared/model/use-map-images.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md,
    src/shared/model/state.ts
  </read_first>
  <action>
    **1. idb-keyval をインストール:**
    ```bash
    bun add idb-keyval
    ```

    **2. `src/shared/model/use-map-images.ts` を作成:**

    ```typescript
    import { del, get, set } from "idb-keyval";

    const PREFIX = "map-img-";

    export const saveMapImage = async (mapId: string, blob: Blob): Promise<void> => {
      try {
        await set(`${PREFIX}${mapId}`, blob);
      } catch {
        // D-06: IndexedDB unavailable — silently fail, image stays null
      }
    };

    export const loadMapImage = async (mapId: string): Promise<Blob | null> => {
      try {
        return (await get<Blob>(`${PREFIX}${mapId}`)) ?? null;
      } catch {
        return null; // D-06: fail gracefully
      }
    };

    export const deleteMapImage = async (mapId: string): Promise<void> => {
      try {
        await del(`${PREFIX}${mapId}`);
      } catch {
        // D-06: ignore
      }
    };
    ```

    3つの関数はすべて `try-catch` で囲み、IndexedDB アクセス不可時はサイレントに失敗する (D-06)。
    `mapId` に `"map-img-"` プレフィックスを付けて idb-keyval のデフォルトストアに保存する。
    `State.maps[].img` フィールドには `mapId` 文字列のみを格納し、Blob は IndexedDB にのみ存在する。

    **3. バレルファイル `src/shared/model/index.ts` に既に export を追加済み (Task 1で実施)。**
    Task 1 のバレルファイルに `export { saveMapImage, loadMapImage, deleteMapImage } from "./use-map-images";` が含まれていることを確認。

  </action>
  <verify>
    <automated>cd /Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/plotted && bun run typecheck && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` の dependencies に `"idb-keyval"` が含まれる
    - `src/shared/model/use-map-images.ts` に `import.*from.*idb-keyval` が含まれる
    - `src/shared/model/use-map-images.ts` に `export const saveMapImage` が含まれる
    - `src/shared/model/use-map-images.ts` に `export const loadMapImage` が含まれる
    - `src/shared/model/use-map-images.ts` に `export const deleteMapImage` が含まれる
    - `src/shared/model/use-map-images.ts` の各関数が try-catch で囲まれている
    - `src/shared/model/index.ts` に `saveMapImage` の re-export が含まれる
    - `bun run typecheck` が exit 0
    - `bun run build` が exit 0
  </acceptance_criteria>
  <done>idb-keyvalがインストールされ、IndexedDBへのマップ画像Blob保存・取得・削除関数が実装されている。IndexedDBアクセス不可時はサイレントに失敗する</done>
</task>

</tasks>

<verification>
1. `bun run typecheck` が成功する
2. `bun run build` が成功する
3. `bunx vitest run --project unit src/shared/model/state.unit.test.ts` が全テスト pass
4. `src/shared/model/` に state.ts, reducer.ts, use-app-state.ts, use-map-images.ts, index.ts が存在する
</verification>

<success_criteria>

- State型が PROJECT.md のデータ構造に準拠して定義されている
- useAppState が useReducer + localStorage 自動保存/復元を提供する
- localStorage 破損時にサイレントリセットされる (D-04, D-05)
- idb-keyval で IndexedDB にマップ画像を CRUD できる
- IndexedDB 不可時にクラッシュしない (D-06)
- すべてのユニットテストが pass する
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
