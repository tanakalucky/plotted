---
phase: 04-indexeddb-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/home/ui/PlottedApp.tsx
autonomous: false
requirements:
  - DATA-01
  - SETUP-03

must_haves:
  truths:
    - "Clicking the reset button deletes all IndexedDB image blobs before clearing state"
    - "After reset, IndexedDB contains no orphaned map-img-* entries"
    - "Reset still works correctly when no maps exist (empty state)"
  artifacts:
    - path: "src/pages/home/ui/PlottedApp.tsx"
      provides: "Reset onClick that cleans IndexedDB before dispatching RESET"
      contains: "deleteMapImage"
  key_links:
    - from: "src/pages/home/ui/PlottedApp.tsx"
      to: "src/shared/model/use-map-images.ts"
      via: "import { deleteMapImage } from @/shared/model"
      pattern: "deleteMapImage"
    - from: "src/pages/home/ui/PlottedApp.tsx"
      to: "IndexedDB"
      via: "Promise.all(state.maps.map(m => deleteMapImage(m.id)))"
      pattern: "Promise\\.all.*deleteMapImage"
---

<objective>
Wire IndexedDB image cleanup into the reset button's onClick handler so that all map image blobs are deleted before state is cleared.

Purpose: Eliminates orphaned IndexedDB blobs on reset, completing the full data reset flow (gap closure for INT-01 from milestone audit).
Output: Modified PlottedApp.tsx with async onClick that awaits IndexedDB cleanup before dispatching RESET.
</objective>

<execution_context>
@/Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/plotted/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/plotted/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/shared/model/index.ts:

```typescript
export { saveMapImage, loadMapImage, deleteMapImage } from "./use-map-images";
export { type State, type MapDef } from "./state";
export { type Action, reducer } from "./reducer";
export { useAppState } from "./use-app-state";
```

From src/shared/model/use-map-images.ts:

```typescript
export const deleteMapImage = async (mapId: string): Promise<void> => {
  try {
    await del(`${PREFIX}${mapId}`);
  } catch {
    // D-06: ignore — silently fails
  }
};
```

From src/shared/model/state.ts (relevant type):

```typescript
// state.maps is MapDef[] where MapDef has { id: string, ... }
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IndexedDB cleanup to reset onClick in PlottedApp.tsx</name>
  <files>src/pages/home/ui/PlottedApp.tsx</files>
  <read_first>
    - src/pages/home/ui/PlottedApp.tsx (current reset onClick at line 92)
    - src/shared/model/use-map-images.ts (deleteMapImage implementation)
    - src/shared/model/index.ts (confirm deleteMapImage is exported)
    - src/features/map-manager/ui/MapHeader.tsx (reference pattern for deleteMapImage usage)
  </read_first>
  <action>
Modify `src/pages/home/ui/PlottedApp.tsx`:

1. **Update import** (line 9): Change `import { useAppState } from "@/shared/model";` to `import { deleteMapImage, useAppState } from "@/shared/model";`

2. **Update reset button onClick** (line 92): Change the synchronous onClick to an async handler that awaits IndexedDB cleanup before dispatching RESET. Per D-01, iterate `state.maps` and call `deleteMapImage` for each, then dispatch. Per D-02, write this inline (no helper function). Per D-03, deleteMapImage already handles errors silently.

Replace:

```typescript
onClick={() => dispatch({ type: "RESET" })}
```

With:

```typescript
onClick={async () => {
  await Promise.all(state.maps.map((m) => deleteMapImage(m.id)));
  dispatch({ type: "RESET" });
}}
```

CRITICAL: The `await Promise.all(...)` MUST execute BEFORE `dispatch({ type: "RESET" })` because after RESET, `state.maps` will be empty and map IDs will be lost.

No other files need modification. No new hooks, helpers, or files per D-02.
</action>
<verify>
<automated>cd /Users/tanakalucky/Work/10.programming/20.private_develop/10.sample-project/plotted && bun run typecheck && bun run lint && bun run test</automated>
</verify>
<acceptance_criteria> - PlottedApp.tsx line 9 contains `import { deleteMapImage, useAppState } from "@/shared/model";` - PlottedApp.tsx contains `await Promise.all(state.maps.map((m) => deleteMapImage(m.id)));` - PlottedApp.tsx contains `dispatch({ type: "RESET" });` AFTER the await line (not before) - PlottedApp.tsx contains `onClick={async () =>` on the destructive reset button - `bun run typecheck` exits 0 - `bun run lint` exits 0 - `bun run test` exits 0
</acceptance_criteria>
<done>Reset button onClick awaits IndexedDB cleanup of all map images before dispatching RESET. Type check, lint, and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify reset cleans IndexedDB</name>
  <files>src/pages/home/ui/PlottedApp.tsx</files>
  <action>Human verifies that the reset button properly cleans IndexedDB entries before clearing state.</action>
  <what-built>IndexedDB image cleanup wired into the reset button. When user clicks "Reset" and confirms, all map image blobs are deleted from IndexedDB before state is cleared.</what-built>
  <how-to-verify>
    1. Open http://localhost:5173 in Chrome
    2. Add 1-2 maps and upload images to them
    3. Open DevTools > Application > IndexedDB > keyval-store > keyval
    4. Confirm entries like "map-img-xxx" exist for each map
    5. Click the "Reset" button in the toolbar, then confirm in the dialog
    6. Check IndexedDB again in DevTools — all "map-img-*" entries should be gone
    7. Verify the app state is fully reset (no characters, maps, or logs)
  </how-to-verify>
  <verify>Human confirms IndexedDB is clean after reset</verify>
  <done>User has verified that reset removes all IndexedDB map image entries</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `bun run typecheck` passes
- `bun run lint` passes
- `bun run test` passes
- PlottedApp.tsx imports `deleteMapImage` from `@/shared/model`
- Reset onClick awaits `Promise.all(state.maps.map(...deleteMapImage...))` before `dispatch({ type: "RESET" })`
- No new files created (per D-02)
</verification>

<success_criteria>

- Dispatching RESET deletes all IndexedDB image blobs for existing maps before resetting state
- After reset, IndexedDB contains no orphaned image entries
- The "Full data reset" E2E flow completes end-to-end without storage leaks
- Zero regressions: typecheck, lint, and existing tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/04-indexeddb-cleanup/04-01-SUMMARY.md`
</output>
